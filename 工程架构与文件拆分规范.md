# 工程架构与文件拆分规范

版本：v0.1  
日期：2026-06-04  
项目：翻译软件 App  
关联文档：`最新技术设计文档.md`、`开发计划与任务.md`

---

## 1. 核心原则

本项目必须避免出现过大的文件、过宽的模块和难以维护的“万能类”。

核心原则：

1. 一个文件只做一类事情。
2. 一个模块只拥有自己的业务边界。
3. 页面只负责布局和交互，不直接写网络、数据库、模型调用。
4. 服务只负责业务编排，不直接承载路由、数据库 SQL、第三方 SDK 细节。
5. Provider 只封装供应商差异，不处理用户、订阅、记录等业务。
6. contracts 是前后端共享边界，不写业务逻辑。
7. 文件变大时先拆分，不等到难以维护再重构。

---

## 2. 文件体积限制

### 2.1 默认上限

| 文件类型 | 建议上限 | 强制拆分线 |
|---|---:|---:|
| Flutter page | 220 行 | 300 行 |
| Flutter widget | 160 行 | 220 行 |
| Flutter controller / notifier | 220 行 | 300 行 |
| Flutter repository | 200 行 | 280 行 |
| Node route | 150 行 | 220 行 |
| Node controller | 180 行 | 250 行 |
| Node service | 240 行 | 320 行 |
| Node repository | 220 行 | 300 行 |
| Provider adapter | 260 行 | 350 行 |
| Python model service file | 240 行 | 320 行 |
| DTO / schema file | 200 行 | 280 行 |
| 单个测试文件 | 280 行 | 380 行 |

### 2.2 函数限制

| 类型 | 建议上限 |
|---|---:|
| 普通函数 | 40 行 |
| 复杂业务函数 | 70 行 |
| Flutter build 方法 | 80 行 |
| API handler | 50 行 |
| Provider event parser | 80 行 |

超过上限时，优先拆成：

- 小组件。
- use case。
- parser。
- mapper。
- validator。
- helper。
- repository 方法。

---

## 3. 仓库结构

推荐使用 monorepo，但每个包职责清晰。

```text
translation-app/
  apps/
    mobile/
  services/
    api-server/
    realtime-gateway/
    worker/
    model-services/
      asr-service/
      translation-service/
      tts-service/
  packages/
    contracts/
    shared-config/
  docs/
  infra/
```

MVP 阶段可以只初始化：

```text
apps/mobile
services/api-server
services/realtime-gateway
packages/contracts
docs
```

---

## 4. Mobile App 架构

### 4.1 目录结构

```text
apps/mobile/lib/
  main.dart
  src/
    app/
      app.dart
      router.dart
      theme.dart
    core/
      config/
      errors/
      logging/
      network/
      storage/
      permissions/
    features/
      auth/
      realtime/
      audio/
      subtitles/
      sessions/
      subscription/
      termbase/
      settings/
```

### 4.2 Feature 内部结构

每个 feature 采用固定分层：

```text
features/realtime/
  presentation/
    pages/
    widgets/
    controllers/
  domain/
    entities/
    use_cases/
  data/
    dtos/
    mappers/
    repositories/
    data_sources/
```

规则：

- `pages/` 只放页面骨架。
- `widgets/` 放可复用 UI 单元。
- `controllers/` 管理页面状态。
- `use_cases/` 放业务动作。
- `repositories/` 封装数据来源。
- `data_sources/` 封装 HTTP、WebSocket、本地数据库。

### 4.3 同传工作台拆分

禁止写成一个 `RealtimePage.dart` 巨型文件。

推荐拆分：

```text
features/realtime/presentation/pages/realtime_page.dart
features/realtime/presentation/widgets/realtime_status_bar.dart
features/realtime/presentation/widgets/subtitle_timeline.dart
features/realtime/presentation/widgets/subtitle_segment_tile.dart
features/realtime/presentation/widgets/realtime_controls.dart
features/realtime/presentation/widgets/audio_level_meter.dart
features/realtime/presentation/widgets/quota_banner.dart
features/realtime/presentation/controllers/realtime_controller.dart
features/realtime/domain/use_cases/start_realtime_session.dart
features/realtime/domain/use_cases/stop_realtime_session.dart
features/realtime/data/data_sources/realtime_socket_data_source.dart
```

页面文件只组合组件：

```text
RealtimePage
  -> RealtimeStatusBar
  -> SubtitleTimeline
  -> RealtimeControls
```

---

## 5. API Server 架构

### 5.1 目录结构

```text
services/api-server/src/
  main.ts
  app.ts
  config/
  infrastructure/
    db/
    redis/
    logger/
    queue/
    storage/
  modules/
    auth/
    users/
    plans/
    usage/
    realtime/
    sessions/
    segments/
    termbase/
    exports/
    billing/
```

### 5.2 Module 内部结构

每个模块固定拆分：

```text
modules/sessions/
  sessions.routes.ts
  sessions.controller.ts
  sessions.service.ts
  sessions.repository.ts
  sessions.schema.ts
  sessions.types.ts
  sessions.errors.ts
  sessions.test.ts
```

规则：

- `routes` 只声明路由和中间件。
- `controller` 只处理 HTTP 输入输出。
- `service` 只处理业务编排。
- `repository` 只处理数据库访问。
- `schema` 只处理请求和响应校验。
- `types` 只放模块内部类型。
- `errors` 只放模块错误码。

### 5.3 禁止事项

禁止：

- 在 controller 里写 SQL。
- 在 route 里调用第三方模型。
- 在 service 里直接解析 HTTP request。
- 在 repository 里写套餐权益判断。
- 把所有 API 写进一个 `routes.ts`。
- 把所有数据库访问写进一个 `db.ts`。

---

## 6. Realtime Gateway 架构

### 6.1 目录结构

```text
services/realtime-gateway/src/
  main.ts
  config/
  connection/
    websocket_server.ts
    client_connection.ts
    connection_registry.ts
  auth/
    realtime_token_verifier.ts
  protocol/
    incoming_event_parser.ts
    outgoing_event_builder.ts
    event_types.ts
  sessions/
    realtime_session.ts
    session_manager.ts
  providers/
    provider_router.ts
    provider_registry.ts
    openai_realtime_provider.ts
    aliyun_realtime_provider.ts
    tencent_realtime_provider.ts
    open_source_provider.ts
  usage/
    usage_ticker.ts
    quota_guard.ts
  metrics/
    realtime_metrics.ts
```

### 6.2 拆分规则

- WebSocket 连接管理和业务会话分开。
- 协议解析和 Provider 调用分开。
- Provider Router 不直接处理 WebSocket。
- Usage Ticker 不直接处理模型事件。
- 每个 Provider 单独文件。
- Provider 的事件解析可再拆成 `xxx_event_mapper.ts`。

### 6.3 Provider 文件限制

当 Provider 文件超过 260 行，拆成：

```text
openai/
  openai_realtime_provider.ts
  openai_session_client.ts
  openai_event_mapper.ts
  openai_error_mapper.ts
  openai_types.ts
```

---

## 7. Model Services 架构

### 7.1 目录结构

```text
services/model-services/
  asr-service/
    app/
      main.py
      config.py
      routes.py
      service.py
      model_loader.py
      schemas.py
      metrics.py
  translation-service/
    app/
      main.py
      routes.py
      service.py
      prompt_builder.py
      model_loader.py
      schemas.py
      termbase.py
  tts-service/
    app/
      main.py
      routes.py
      service.py
      model_loader.py
      audio_stream.py
      schemas.py
```

### 7.2 Python 文件拆分规则

禁止把模型加载、HTTP route、推理、音频处理、指标上报写在一个 `main.py`。

规则：

- `main.py` 只创建应用和注册路由。
- `routes.py` 只处理 HTTP/gRPC 输入输出。
- `service.py` 只做推理编排。
- `model_loader.py` 只加载模型。
- `schemas.py` 只定义请求响应结构。
- `metrics.py` 只做指标。

---

## 8. Contracts 包

### 8.1 目录结构

```text
packages/contracts/src/
  realtime/
    events.ts
    audio.ts
    session.ts
    errors.ts
  api/
    auth.ts
    sessions.ts
    usage.ts
    billing.ts
    termbase.ts
  shared/
    languages.ts
    plans.ts
    pagination.ts
```

### 8.2 规则

- contracts 只放类型、schema、常量、错误码。
- 不放业务逻辑。
- 不依赖 API Server。
- 不依赖 Flutter。
- 不依赖模型 SDK。

---

## 9. 拆分触发条件

出现以下情况必须拆分：

1. 文件超过强制拆分线。
2. 一个文件里出现 3 个以上不相关职责。
3. 一个函数同时做校验、数据库、第三方调用、响应格式化。
4. 一个页面里出现 5 个以上明显可复用 UI 区块。
5. 一个 service 同时服务多个业务模块。
6. 一个 Provider 同时处理 session、event mapping、error mapping、usage mapping。
7. 测试文件里 setup 超过 80 行。

---

## 10. 命名规范

### 10.1 Flutter

```text
realtime_page.dart
subtitle_timeline.dart
realtime_controller.dart
start_realtime_session.dart
realtime_repository.dart
```

### 10.2 TypeScript

```text
sessions.routes.ts
sessions.controller.ts
sessions.service.ts
sessions.repository.ts
sessions.schema.ts
provider-router.ts
usage-ticker.ts
```

### 10.3 Python

```text
model_loader.py
prompt_builder.py
audio_stream.py
asr_service.py
tts_service.py
```

---

## 11. 代码评审检查清单

每次合并前检查：

- 是否新增超过 300 行的业务文件。
- 是否有 controller 直接访问数据库。
- 是否有页面直接调用 HTTP/WebSocket。
- 是否有 Provider 写入业务表。
- 是否有重复 DTO。
- 是否有未抽出的巨大 build 方法。
- 是否有难以单测的长函数。
- 是否有跨模块直接访问内部文件。

---

## 12. 自动化约束

后续工程初始化后建议配置：

### 12.1 TypeScript

- ESLint `max-lines`。
- ESLint `max-lines-per-function`。
- ESLint `complexity`。
- 禁止跨模块深层 import。

### 12.2 Flutter

- `dart analyze`。
- 自定义 lint 或 review 规则检查大文件。
- 页面 build 方法超过阈值时阻断合并。

### 12.3 Python

- Ruff。
- Pyright 或 mypy。
- 单文件行数检查。
- 单函数复杂度检查。

### 12.4 CI

CI 必须检查：

```text
lint
typecheck
test
max-file-lines
max-function-lines
forbidden-imports
```

---

## 13. 开发顺序建议

初始化工程时先建立边界：

1. 创建 monorepo 目录。
2. 创建 contracts 包。
3. 创建 API Server 模块结构。
4. 创建 Realtime Gateway 模块结构。
5. 创建 Flutter feature 结构。
6. 创建空 Provider Adapter。
7. 创建 lint 和 CI。
8. 再开始写业务代码。

这样可以从第一天避免大文件。

