# 移动端跨平台适配规范

版本：v0.1  
日期：2026-06-04  
项目：翻译软件 App  
目标：一套 App 代码同时适配 iOS 和 Android，避免双端重复开发

---

## 1. 核心决策

移动端采用 Flutter 单代码库开发。

目标：

- iOS 和 Android 共用 85%-90% 以上业务代码。
- 页面、状态管理、网络、WebSocket、会话记录、订阅展示、术语库、设置等全部共用。
- 平台差异只放在薄原生适配层。
- 禁止分别开发一套 iOS App 和一套 Android App。

---

## 2. 适配原则

### 2.1 共用代码优先

必须共用：

- 页面 UI。
- 状态管理。
- 路由。
- API client。
- Realtime WebSocket client。
- 字幕渲染。
- 会话记录。
- 术语库。
- 用量和套餐展示。
- 导出逻辑。
- 错误码和提示文案。

### 2.2 原生能力最小化

只在必要场景写平台适配：

- 麦克风权限。
- 音频采集。
- 音频播放和音频会话。
- iOS StoreKit。
- Google Play Billing。
- 文件分享/导出。
- 推送通知。
- 崩溃监控平台配置。

### 2.3 禁止双端分叉

禁止：

- iOS 和 Android 分别写两套页面。
- iOS 和 Android 分别写两套实时同传业务逻辑。
- 在页面里大量 `if iOS else Android`。
- 为某个平台复制整个 feature。
- 两端分别维护不同 API DTO。
- 两端分别维护不同 Provider 协议。

允许：

- 小范围平台条件样式。
- 原生权限和音频配置差异。
- 支付 SDK 差异。
- 系统分享和文件路径差异。

---

## 3. 移动端架构

### 3.1 目录结构

```text
apps/mobile/
  lib/
    main.dart
    src/
      app/
      core/
      features/
      platform/
  ios/
  android/
```

平台适配统一放在：

```text
lib/src/platform/
  audio/
    audio_capture.dart
    audio_capture_platform.dart
    audio_capture_method_channel.dart
  permissions/
    microphone_permission.dart
  billing/
    app_purchase_service.dart
    apple_purchase_adapter.dart
    google_purchase_adapter.dart
  sharing/
    file_share_service.dart
```

规则：

- `features/` 不直接调用原生 SDK。
- `features/` 只依赖 `platform/` 暴露的统一接口。
- `platform/` 内部可以判断 iOS/Android。
- `ios/` 和 `android/` 目录只放必要原生配置和少量插件代码。

---

## 4. 平台抽象接口

### 4.1 音频采集接口

业务层只依赖统一接口：

```dart
abstract interface class AudioCapture {
  Stream<AudioFrame> get frames;
  Future<void> requestPermission();
  Future<void> start(AudioCaptureConfig config);
  Future<void> pause();
  Future<void> resume();
  Future<void> stop();
}
```

iOS / Android 差异在实现层处理：

- iOS：Audio Session、麦克风权限、后台行为。
- Android：RECORD_AUDIO、前台服务限制、音频焦点。

### 4.2 支付接口

```dart
abstract interface class PurchaseService {
  Future<List<PlanProduct>> loadProducts();
  Future<PurchaseResult> purchase(String planCode);
  Stream<PurchaseUpdate> get updates;
  Future<void> restorePurchases();
}
```

实现：

- iOS：StoreKit。
- Android：Google Play Billing。
- 国内安卓渠道：后续新增渠道 adapter，不改页面和业务状态。

### 4.3 文件导出接口

```dart
abstract interface class FileShareService {
  Future<void> shareFile(String path, {String? mimeType});
  Future<String> saveExportFile(List<int> bytes, String filename);
}
```

---

## 5. UI 适配规范

### 5.1 响应式布局

必须适配：

- 小屏 iPhone。
- 大屏 iPhone。
- Android 常见 6-7 英寸屏。
- 平板可用，但 MVP 不专门做平板优化。

规则：

- 不写固定绝对高度承载大量字幕。
- 字幕区使用弹性布局。
- 控制栏固定高度。
- 安全区使用 SafeArea。
- 字体大小通过设置控制，不随屏幕宽度任意缩放。

### 5.2 平台视觉差异

MVP 使用统一视觉风格。

允许：

- iOS 使用 Cupertino 风格弹窗或系统选择器。
- Android 使用 Material 风格权限引导。

禁止：

- iOS/Android 两套完全不同页面。
- 双端按钮文案、流程、功能入口不同。

---

## 6. 音频适配重点

### 6.1 iOS

必须处理：

- 麦克风权限说明。
- Audio Session 分类。
- 静音开关和扬声器输出。
- 耳机和蓝牙设备。
- App 切后台后的行为限制。

MVP 策略：

- 只承诺前台同传。
- 切后台时暂停或提示用户返回。
- 不做系统通话内翻译。

### 6.2 Android

必须处理：

- RECORD_AUDIO 权限。
- 不同厂商 ROM 权限差异。
- 音频焦点。
- 蓝牙和有线耳机。
- 长时间录音的前台服务限制。

MVP 策略：

- 只承诺前台同传。
- 如需后台继续收音，后续单独设计前台服务和通知。

---

## 7. 支付适配重点

### 7.1 共用业务

共用：

- 套餐展示。
- 权益解释。
- 订阅状态。
- 剩余分钟。
- 购买成功后的权益刷新。

### 7.2 平台差异

iOS：

- StoreKit 购买。
- Restore Purchases。
- 后端校验 Apple 交易。

Android：

- Google Play Billing。
- purchase token 后端校验。
- 订阅状态同步。

国内安卓：

- 微信/支付宝/渠道支付后续通过 adapter 扩展。
- 不影响现有套餐页和权益模块。

---

## 8. 测试矩阵

### 8.1 设备矩阵

最低测试：

| 平台 | 设备 |
|---|---|
| iOS | 小屏 iPhone |
| iOS | 大屏 iPhone |
| Android | Pixel / 原生 Android |
| Android | 国产常见 ROM |
| Android | 中低端设备 |

### 8.2 必测场景

每个平台都必须通过：

- 首次启动。
- 麦克风授权。
- 拒绝权限后的引导。
- 开始同传。
- 暂停/恢复/结束。
- 30 分钟会话。
- 弱网重连。
- 字幕滚动。
- 译文朗读开关。
- 历史记录。
- 导出。
- 订阅购买。
- 恢复购买。

---

## 9. 防止重复开发的工程规则

### 9.1 PR 检查

每次移动端 PR 必须检查：

- 是否把业务逻辑写进 `ios/` 或 `android/`。
- 是否复制了整个 feature。
- 是否出现大量平台判断。
- 是否新增重复 DTO。
- 是否新增双端不同流程。

### 9.2 文件边界

平台代码只允许出现在：

```text
lib/src/platform/
ios/
android/
```

业务 feature 不允许直接 import：

```text
dart:io
MethodChannel
StoreKit 相关实现
Google Billing 相关实现
```

业务层必须通过接口调用。

---

## 10. 开发顺序

移动端开发顺序：

1. 建立 Flutter 工程。
2. 建立 feature 目录结构。
3. 建立 platform 抽象接口。
4. 先实现 mock platform adapter。
5. 完成同传工作台 UI。
6. 接入真实音频采集。
7. 接入 Realtime WebSocket。
8. 接入字幕渲染。
9. 接入支付 adapter。
10. 做 iOS/Android 双端回归。

---

## 11. 验收标准

MVP 移动端验收：

- iOS 和 Android 共用同一套 Flutter feature。
- 两端主流程一致。
- 双端都能完成实时同传。
- 双端都能保存和查看记录。
- 双端都能完成订阅测试。
- 平台差异集中在 `platform/` 和原生配置层。
- 不存在复制出来的 iOS/Android 两套业务模块。

