# 无界AI 1.1 S4 开发检查点

2026-09-09。用户授权“下一批，然后push”。本检查点整理当前开发分支累计的S1–S4增量与第76批Qwen非实时兼容ASR，不是1.1正式发版。冻结1.0提交898ee517e7aac00b03bc79ff2d0597dd00fdbf56及树5c564f99489f7bd62eae28ef75157f6e77a2c369保持不变。

## 已纳入的源码范围

- 复用原App、界面、Controller、历史和音频协调器，接入Apple Speech、原Silero VAD、系统翻译与系统朗读资源准备；本地与在线按既定两种模式隔离。
- 原账号、结果授权同步、会话水位、停止/唯一结算、调用意图与未知用量状态；公有模型调用必须绑定同一部署、配置revision、用户授权与lease。
- 原服务端公有/私有配置页，手填Qwen、腾讯、OpenAI、Google参数；加密保存与凭据隔离，不内置真实密钥或固定采购型号。
- 原ASR/MT/TTS工厂和输出队列中的内部协议候选，见[配置运行手册](MODEL_CONFIGURATION_RUNBOOK.md)。在线模式不静默回落端侧或私有模型。
- 第76批：Qwen完整PCM经原WAV工具转内联数据，兼容chat/completions，明确语言、16k/24k、最多30秒；复用取消、限时、调用记录和防重放，不新建业务Provider，不将完整文件接口标成实时流式。

## 验收层级与剩余门禁

本检查点测试使用合成音频、虚构凭据和模拟供应商响应。源代码与主机测试通过不能替代真实服务商、真机或发行验收。

- S4仍在进行中。下一步按既有计划收敛协议能力矩阵，再推进公有create/准入和采样率发行接线；真实模型资格、预算/同意、凭据轮换和未决调用恢复仍有门禁。
- 自动语种、自动反向及其他尚未取得资格的组合不得标记完成。用户暂不新增声学语言模型的决定保持。
- 本轮不安装手机、不调用付费或真实模型、不部署服务、不迁移/清理用户数据、不合并主分支、不创建发行标签或发布安装包。
- 模型权重、签名、设备配置、会话记录及本机运维/停用诊断脚本不在此源码提交中；保留在原本地位置，没有删除。原Silero暂存脚本按已审查本地缓存的固定哈希取资源。
- 冻结1.0无需随本检查点改动或重新签名。后续回滚须另行确认具体部署目标；本轮没有部署，因此不执行运行环境回滚。

## 推送前检查

按已有CI执行Node全项目构建/类型检查/测试、350行限制、双版本策略，以及Flutter分析与测试。超长文件只做职责拆分，未放宽CI阈值；机密扫描针对本次提交及尚未公开的提交范围。详细本机原始日志不上传公开仓库；最终主机结果和远端CI状态由本轮验收报告单独记录。

本轮最终主机结果：Node全项目560个测试文件、2,860项测试通过（Gateway824、API933、contracts80包含在其中），所有工作区构建和类型检查通过；Flutter752项通过，规范修正后相关24项再次通过，最终分析零问题；350行门禁与双版本40项策略通过。暂存增量和未公开历史范围Gitleaks均无泄漏检出。309个代码/测试文件与实际验证快照SHA256一致。主机使用Node25.8.2；GitHub工作流固定Node24，远端CI结果须单独核验，不由此替代。
