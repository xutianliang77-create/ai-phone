# 详细开发计划

版本：v0.1  
日期：2026-06-04  
项目：中英实时同传工作站 App  
关联文档：`最新技术设计文档.md`、`开发计划与任务.md`、`工程架构与文件拆分规范.md`、`移动端跨平台适配规范.md`

---

## 1. 开发目标

用 10 周完成 MVP 到上架候选版。

MVP 必须支持：

- iOS / Android 单代码库 App。
- 麦克风授权和音频采集。
- 中英实时双语字幕。
- 可选译文朗读。
- 会话保存和历史记录。
- 基础导出。
- 免费分钟和订阅权益。
- Provider Router。
- 至少一个实时商业模型链路。
- 模型 POC 报告。

---

## 2. 研发原则

1. 不使用 CodeClaw。
2. Flutter 单代码库，禁止 iOS/Android 双端重复业务开发。
3. 新工程从第一天按模块拆分，避免大文件。
4. 字幕优先，语音播报可延后。
5. Provider 可插拔，不能把模型供应商写死在 App。
6. 客户端不保存供应商 API Key。
7. 默认不保存原始录音。
8. 先 POC，再做完整产品闭环。

---

## 3. 团队分工

| 角色 | 职责 |
|---|---|
| 产品 / UI | MVP 范围、页面原型、权限文案、套餐文案、上架文案 |
| 移动端 | Flutter App、音频采集、字幕渲染、支付、双端适配 |
| 后端 | API Server、Realtime Gateway、数据库、用量、订阅、导出 |
| AI / 模型 | Provider 接入、模型 POC、测试集、延迟和成本评估 |
| 测试 | 测试用例、音频测试、弱网测试、支付测试、发布回归 |

如果团队人数不足，优先顺序：

```text
实时链路 POC
-> 产品闭环
-> 用量和订阅
-> 上架准备
-> 开源模型增强
```

---

## 4. 工作流

### 4.1 分支策略

```text
main              # 稳定分支
develop           # 日常集成
feature/*         # 功能分支
fix/*             # 修复分支
release/*         # 发布候选分支
```

### 4.2 提交流程

每个任务：

1. 从任务清单领取 ID。
2. 创建 feature 分支。
3. 完成代码和自测。
4. 更新相关测试。
5. 提交 PR。
6. 通过 lint / typecheck / test。
7. 合并到 develop。

### 4.3 Definition of Done

任务完成必须满足：

- 功能符合验收标准。
- 没有超出文件拆分规范的巨型文件。
- 关键逻辑有测试。
- 错误状态有处理。
- 日志不泄露敏感数据。
- 文档或 contracts 同步更新。

---

## 5. Sprint 计划

### Sprint 0：准备和架构基线

时间：第 1 周。

目标：

- 建立工程边界。
- 建立最小可运行工程。
- 冻结 MVP 范围。

交付物：

- monorepo 目录。
- Flutter 最小 App。
- API Server health check。
- Realtime Gateway health check。
- contracts 初版。
- CI 初版。
- UI 低保真原型。

任务：

| ID | 任务 | 负责人 | 验收 |
|---|---|---|---|
| S0-01 | 创建 monorepo | 后端 | 目录符合拆分规范 |
| S0-02 | 初始化 contracts 包 | 后端 | realtime event 类型可导出 |
| S0-03 | 初始化 Flutter App | 移动端 | iOS/Android 可启动 |
| S0-04 | 建立 platform 抽象接口 | 移动端 | audio/billing/sharing 有 mock adapter |
| S0-05 | 初始化 API Server | 后端 | `/health` 返回 ok |
| S0-06 | 初始化 Realtime Gateway | 后端 | WebSocket 可连接 |
| S0-07 | 建立 lint/test/build CI | 后端/移动端 | PR 自动检查 |
| S0-08 | 完成核心页面低保真 | 产品/UI | 同传、记录、订阅、我的页完成 |

出口标准：

- 工程能本地启动。
- CI 可运行。
- 没有任何业务巨型文件。

---

### Sprint 1：实时链路 POC

时间：第 2-3 周。

目标：

- 跑通最小实时同传 Demo。

交付物：

- App 麦克风采集。
- Realtime WebSocket。
- 第一个 Provider。
- 字幕渲染。
- 3 分钟 Demo。

任务：

| ID | 任务 | 负责人 | 验收 |
|---|---|---|---|
| S1-01 | 实现麦克风权限流程 | 移动端 | 授权/拒绝/设置引导通过 |
| S1-02 | 实现 PCM 音频采集 | 移动端 | 可发送 audio.frame |
| S1-03 | 实现 Realtime 客户端 | 移动端 | 可连接、断开、重连 |
| S1-04 | 实现实时 token 校验 | 后端 | 无效 token 被拒绝 |
| S1-05 | 接入 OpenAI 或国内 Provider | AI/后端 | 能返回字幕事件 |
| S1-06 | 实现字幕时间线组件 | 移动端 | partial/final 正确展示 |
| S1-07 | 实现 usage.tick 雏形 | 后端 | 每 30 秒推送用量 |
| S1-08 | Demo 自测 | 全员 | 3 分钟连续同传 |

出口标准：

- 首字反馈 <= 1 秒。
- 译文字幕 <= 3 秒。
- 可连续演示 3 分钟。

---

### Sprint 2：模型 POC 和路由

时间：第 3-4 周。

目标：

- 比较商业和开源模型链路。
- 确认 MVP 默认模型策略。

交付物：

- Provider Router 初版。
- 60 条音频测试集。
- OpenAI/阿里/腾讯/开源链路 POC 数据。
- 模型 POC 报告。

任务：

| ID | 任务 | 负责人 | 验收 |
|---|---|---|---|
| S2-01 | 实现 Provider Router | 后端 | 按地区/套餐/开关路由 |
| S2-02 | 接入 OpenAI POC | AI/后端 | 测试集可跑 |
| S2-03 | 接入阿里 POC | AI/后端 | 测试集可跑 |
| S2-04 | 接入腾讯 POC | AI/后端 | 测试集可跑或完成可行性结论 |
| S2-05 | 准备音频测试集 | 测试/产品 | 60 条样本完成 |
| S2-06 | 部署 SenseVoice POC | AI | 可输出 transcript |
| S2-07 | 部署 Gemma/Qwen 翻译 POC | AI | 可翻译中英文本 |
| S2-08 | 输出模型 POC 报告 | AI/产品 | 明确默认和备用 Provider |

出口标准：

- 海外默认 Provider 有结论。
- 国内默认 Provider 有结论。
- 开源链路是否进入 MVP 有结论。
- 每分钟成本有初步估算。

---

### Sprint 3：产品闭环

时间：第 4-6 周。

目标：

- 用户可以完整使用和复盘。

交付物：

- 登录/游客试用。
- 会话保存。
- 历史记录。
- 用量扣减。
- 基础导出。

任务：

| ID | 任务 | 负责人 | 验收 |
|---|---|---|---|
| S3-01 | 实现 Auth | 后端/移动端 | guest/login/refresh/logout 可用 |
| S3-02 | 实现 sessions 表和 API | 后端 | 会话可创建/结束/查询 |
| S3-03 | 实现 segments 保存 | 后端 | 字幕片段可入库 |
| S3-04 | 实现历史记录列表 | 移动端 | 可查看历史 |
| S3-05 | 实现会话详情页 | 移动端 | 原文/译文展示 |
| S3-06 | 实现用量余额 | 后端 | 扣减和查询正确 |
| S3-07 | 实现余额不足限制 | 后端/移动端 | 不可继续新会话 |
| S3-08 | 实现 TXT/Markdown 导出 | 后端/移动端 | 文件可生成和分享 |
| S3-09 | 实现基础设置 | 移动端 | 字幕/朗读/保存设置可用 |

出口标准：

- 用户可完成“开始同传 -> 结束 -> 查看记录 -> 导出”。
- 免费额度不能绕过。

---

### Sprint 4：订阅支付

时间：第 6-7 周。

目标：

- 建立付费权益闭环。

交付物：

- 套餐页。
- iOS IAP。
- Google Play Billing。
- 后端交易校验。
- 订阅 webhook。

任务：

| ID | 任务 | 负责人 | 验收 |
|---|---|---|---|
| S4-01 | 实现 plans 配置 | 后端 | App 可获取套餐 |
| S4-02 | 实现套餐页 | 移动端 | 权益展示清楚 |
| S4-03 | 接入 iOS IAP | 移动端 | 沙盒购买成功 |
| S4-04 | 接入 Google Play Billing | 移动端 | 测试购买成功 |
| S4-05 | Apple 交易校验 | 后端 | 后端权益更新 |
| S4-06 | Google 交易校验 | 后端 | 后端权益更新 |
| S4-07 | 订阅 webhook | 后端 | 幂等处理 |
| S4-08 | 支付异常提示 | 移动端 | 取消/失败/恢复购买提示正确 |

出口标准：

- 支付成功后权益实时生效。
- 权益以后端为准。
- 支付失败不错误开通权益。

---

### Sprint 5：稳定性和体验

时间：第 7-9 周。

目标：

- 达到 Beta 可测质量。

交付物：

- 弱网重连。
- 异常状态。
- 崩溃监控。
- 30 分钟稳定会话。
- Beta 包。

任务：

| ID | 任务 | 负责人 | 验收 |
|---|---|---|---|
| S5-01 | 弱网重连 | 后端/移动端 | 短断线可恢复 |
| S5-02 | Provider fallback | 后端 | 主 Provider 故障可切换 |
| S5-03 | 语音播放队列优化 | 移动端 | TTS 不阻塞字幕 |
| S5-04 | 错误码和用户提示 | 后端/移动端 | 主要错误可理解 |
| S5-05 | 接入日志和 trace_id | 后端 | 会话可追踪 |
| S5-06 | 接入崩溃监控 | 移动端 | 崩溃可上报 |
| S5-07 | 30 分钟稳定性修复 | 全员 | 通过稳定性测试 |
| S5-08 | Beta 包构建 | 移动端 | TestFlight/Android 内测包 |

出口标准：

- 30 分钟会话稳定。
- 核心异常不丢记录。
- Beta 用户可安装测试。

---

### Sprint 6：发布准备

时间：第 9-10 周。

目标：

- 形成上架候选版。

交付物：

- 隐私政策。
- 用户协议。
- 上架素材。
- Release Candidate。

任务：

| ID | 任务 | 负责人 | 验收 |
|---|---|---|---|
| S6-01 | 完成隐私政策 | 产品/法务 | 明确云端音频处理 |
| S6-02 | 完成用户协议 | 产品/法务 | 包含翻译免责 |
| S6-03 | 配置权限说明 | 移动端 | iOS/Android 权限文案完整 |
| S6-04 | 上架截图和描述 | 产品/UI | 双平台素材完成 |
| S6-05 | 许可证审查 | 后端/AI | 模型和依赖风险清单 |
| S6-06 | 发布回归 | 测试 | P0/P1 问题清零 |
| S6-07 | 构建 RC 包 | 移动端 | 可提交审核 |

出口标准：

- Release Candidate 可提交审核。
- 发布阻断问题为 0。

---

## 6. 详细任务包

### 6.1 移动端任务包

必须按 feature 拆分，禁止双端业务分叉。

任务包：

1. App shell：启动、路由、主题、错误边界。
2. Platform abstraction：audio、billing、sharing、permissions。
3. Realtime：WebSocket、状态机、字幕渲染、控制栏。
4. Sessions：历史列表、详情、导出入口。
5. Subscription：套餐、购买、恢复购买、权益展示。
6. Settings：字幕、朗读、隐私设置。

关键验收：

- iOS/Android 共用同一套 feature。
- 平台差异只在 `platform/`。
- 同传页面文件不超过拆分线。

### 6.2 后端任务包

任务包：

1. API shell：配置、日志、错误格式、OpenAPI。
2. Auth：游客、登录、刷新、注销。
3. Usage：余额、扣减、重置、用量记录。
4. Sessions：会话、片段、记录、导出。
5. Billing：plan、交易校验、webhook、权益。
6. Termbase：术语库 CRUD、导入导出。

关键验收：

- controller 不写数据库逻辑。
- service 不直接处理 HTTP。
- repository 不写业务判断。

### 6.3 Realtime 任务包

任务包：

1. Connection：WebSocket 接入、连接注册。
2. Protocol：事件解析、事件构建、错误码。
3. Session：状态机、暂停、恢复、结束。
4. Provider：Router、Provider registry、event mapper。
5. Usage：计时、扣减、余额不足。
6. Metrics：延迟、错误、fallback。

关键验收：

- Provider 差异不泄漏到 App。
- 字幕事件格式统一。
- usage.tick 准确。

### 6.4 AI 任务包

任务包：

1. 商业实时链路 POC。
2. 国内实时链路 POC。
3. 开源字幕链路 POC。
4. 音频测试集。
5. 成本和质量评估。
6. Provider 路由建议。

关键验收：

- POC 报告能支持产品定价和默认路由决策。

---

## 7. 开发检查点

每周检查：

- 关键路径是否延误。
- 是否出现大文件。
- 双端是否出现业务分叉。
- Provider 是否可替换。
- 用量是否可控。
- 测试是否跟上。

每个 Sprint 结束必须输出：

- 已完成任务。
- 未完成任务。
- 阻塞问题。
- 风险更新。
- 下一 Sprint 调整。

---

## 8. 交付物清单

最终交付：

- Flutter iOS/Android App。
- Node.js API Server。
- Node.js Realtime Gateway。
- Python 模型服务 POC。
- contracts 包。
- 数据库迁移。
- OpenAPI 文档。
- 模型 POC 报告。
- 测试报告。
- 隐私政策和用户协议。
- App Store / Google Play 上架资料。

